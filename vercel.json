{
  "_comment": "Minimal Vercel config - auto-detects api/index.py",
  "buildCommand": "cd client && npm ci && npm run build",
  "outputDirectory": "client/dist",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
