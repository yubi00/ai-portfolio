{
  "name": "ai-portfolio-terminal",
  "private": true,
  "workspaces": ["client", "server"],
  "scripts": {
    "bootstrap": "npm -w client install && npm -w server install",
  "dev": "concurrently -k -n client,server -c blue,green \"npm run -w client dev\" \"npm run -w server dev\"",
    "build": "npm run -w client build && npm run -w server build",
    "lint": "npm run -w client lint && npm run -w server lint"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
